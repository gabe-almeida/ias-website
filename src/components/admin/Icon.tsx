import React from "react";

// Compact IAS mark shown in the collapsed admin sidebar / nav.
export const Icon = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img src="/assets/ias-logo.png" alt="IAS" style={{ height: 26, width: "auto" }} />
);

export default Icon;
